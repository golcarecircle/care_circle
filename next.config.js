/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'images.pexels.com',
      'randomuser.me',
      'source.unsplash.com',
      'img.freepik.com',
    ],
    
  },
}

module.exports = nextConfig
